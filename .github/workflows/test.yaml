name: TEst

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ contains(github.event.head_commit.modified, 'deployment/') }}
    steps:
      - run: echo "hej från deploy"
  docker:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event }}
